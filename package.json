{
	"name": "webglrenderingengine",
	"version": "1.0.0",
	"description": "",
	"main": "main.js",
	"scripts": {
		"build": "npx tsc && cp src/index.html dist/index.html && cp -R src/assets dist/",
		"watch": "npx tsc --watch",
		"serve": "http-server ./dist -c-1",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Hyumiris",
	"license": "ISC",
	"dependencies": {
		"@types/gl-matrix": "^2.4.5",
		"@types/requirejs": "^2.1.34",
		"rxjs": "^7.5.1",
		"typescript": "^4.5.4"
	}
}
